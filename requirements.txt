fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
faster-whisper>=1.0.0
edge-tts>=6.1.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0
groq>=0.9.0
litellm>=1.40.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
xlrd>=2.0.1
pytesseract>=0.3.10
Pillow>=10.0.0

# Video ingestion. ffmpeg/ffprobe themselves are NOT a pip package -- install
# the system binary separately (e.g. `apt-get install ffmpeg`), same as
# Tesseract. scenedetect is optional: video_service.py falls back to
# fixed-interval frame sampling if it isn't installed.
scenedetect[opencv]>=0.6.4
psycopg2-binary>=2.9.9
beautifulsoup4>=4.12.0